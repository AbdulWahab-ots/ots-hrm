// General Schedule Type Enum
export enum ScheduleType {
    REGULAR = "REGULAR",
    OVERTIME = "OVERTIME", 
    FLEXIBLE = "FLEXIBLE",
    CUSTOM = "CUSTOM"
}

export enum WorkType {
    REMOTE = "REMOTE",
    ONSITE = "ONSITE",
}

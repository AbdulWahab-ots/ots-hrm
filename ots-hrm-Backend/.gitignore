# node_modules ignored as it packs dependencies
node_modules


# build files and folders to ignore
dist

# environment files to ignore (keep .env.example tracked as the template)
.env
.env.*
!.env.example
env *

